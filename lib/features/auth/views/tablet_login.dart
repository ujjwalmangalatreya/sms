import 'package:flutter/widgets.dart';

class TabletLoginPage extends StatelessWidget {
  const TabletLoginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Tablet Login Page"),);
  }
}