import 'package:flutter/material.dart';

class AdmissionLetter extends StatelessWidget {
  const AdmissionLetter({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Admission LETTER student"),
    );
  }
}
