import 'package:flutter/material.dart';

class ManageloginStudent extends StatelessWidget {
  const ManageloginStudent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MANAGE LOGIN STUDENT"),
    );
  }
}
