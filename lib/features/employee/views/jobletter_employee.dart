import 'package:flutter/material.dart';

class JobletterEmployee extends StatelessWidget {
  const JobletterEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("JOB LETTER EMPLOYEE"),
    );
  }
}
