import 'package:flutter/material.dart';

class ManageloginEmployee extends StatelessWidget {
  const ManageloginEmployee({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("MANAGE LOGIN"),
    );
  }
}
